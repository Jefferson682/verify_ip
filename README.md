# verify_ip
Checks the IP and send notification.

Requirements:
- Python
- SQLite

Run project:
```shell
python3 -m venv .venv
```
```shell
pip install -r requirements
```
```shell
python3 verify_ip.py
```